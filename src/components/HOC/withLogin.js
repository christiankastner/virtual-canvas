import React from 'react'

const UpdatedModal = (OrginialModal) => {
    class NewModal extends React.Component {
        const
        render() {
            return <OriginalModal />
        }
    }
    return NewModal
}

export default UpdatedModal()